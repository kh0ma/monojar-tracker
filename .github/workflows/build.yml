name: Build and push Docker image

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies and run tests
        run: npm ci && npm test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3.3.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6.6.1
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mono-jar:latest
            ghcr.io/${{ github.repository_owner }}/mono-jar:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/mono-jar:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/mono-jar:1.${{ github.run_number }}.${{ github.run_attempt }}-${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
